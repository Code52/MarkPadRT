<Page
    x:Class="MarkPad.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:MarkPad="using:MarkPad"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Page.Resources>
        <MarkPad:BooleanToVisibilityConverter  x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>
    <Page.TopAppBar>
        <AppBar  Background="#494949">
            <StackPanel Orientation="Horizontal">
                <Button Width="100" Height="100" Foreground="White" VerticalContentAlignment="Bottom" Content="New" Command="{Binding NewCommand}"/>
                <Button Width="100" Height="100" Foreground="White" VerticalContentAlignment="Bottom" Content="Open" Command="{Binding OpenCommand}" />
                <Button Width="100" Height="100" Foreground="White" VerticalContentAlignment="Bottom" Content="Open From Github" Command="{Binding OpenGithubCommand}" />
                <Button Width="100" Height="100" Foreground="White" VerticalContentAlignment="Bottom" Content="Save" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </AppBar>
    </Page.TopAppBar>
    <Page.BottomAppBar>
        <AppBar  Background="#494949">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <Button Foreground="White" Style="{StaticResource BoldAppBarButtonStyle}" Click="BoldClicked" />
                    <Button Foreground="White" Style="{StaticResource ItalicAppBarButtonStyle}"  Click="ItalicClicked"/>
                    <Button Foreground="White" Style="{StaticResource FontIncreaseAppBarButtonStyle}" Click="HeaderClicked" />
                    <Button Foreground="White" Style="{StaticResource LinkAppBarButtonStyle}"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Foreground="White" AutomationProperties.Name="Distraction Free" Style="{StaticResource ClosePaneAppBarButtonStyle}" />
                </StackPanel>
            </Grid>
        </AppBar>
    </Page.BottomAppBar>

    <Grid Background="#f9f9f9">
        <Grid Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="40*" />
                <ColumnDefinition Width="40*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Background="White">
                <TextBlock HorizontalAlignment="Left" TextWrapping="Wrap" VerticalAlignment="Top" Margin="18,13,0,0" Foreground="#FFBE0606" Style="{StaticResource PageHeaderTextStyle}" Text="MarkPad"/>
                <ListBox x:Name="Documents" BorderBrush="{x:Null}" BorderThickness="0" ItemsSource="{Binding Documents}" SelectedItem="{Binding SelectedDocument, Mode=TwoWay}" Margin="18,86,0,0" SelectionChanged="DocumentsSelectionChanged">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" />
                                <TextBlock Text="*" Visibility="{Binding IsModified, Converter={StaticResource BooleanToVisibilityConverter}}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <RichEditBox x:Name="Editor" Grid.Column="1" Background="#f9f9f9" BorderThickness="0" BorderBrush="{x:Null}" Padding="25, 25, 25, 0" TextChanged="TextChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled" AcceptsReturn="True"  />
            <Border Background="#eaeaea" Grid.Column="2" >
                <WebView x:Name="wv" Margin="15,15,15,0" />
            </Border>
            <Rectangle x:Name="webRectangle" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Column="2"  Margin="15,15,15,0" />
        </Grid>
    </Grid>
</Page>
